llm:
  # Optional override when your Codex auth cache is not at ~/.codex/auth.json.
  # Copy this file to configs/local.yaml only if you need a local override.
  # auth_file_path: /absolute/path/to/auth.json
